---
services: docker

env:
  global:
    - DOCKER_USER: qwe1
    - DOCKER_IMAGE: checkdmarc
    - GITLAB_REG_URL: registry.gitlab.com
    - GITLAB_REG_USER: richardskumat
    - GITLAB_REG_IMAGE: checkdmarc
  #matrix:
  #  - whatever

before_script:
  - docker info

script:
  # Run tests.
  - docker build -t $DOCKER_USER/$DOCKER_IMAGE:travis-$DOCKER_IMAGE .
  - docker images
  - docker run $DOCKER_USER/$DOCKER_IMAGE:travis-$DOCKER_IMAGE google.com
  - docker run $DOCKER_USER/$DOCKER_IMAGE:latest facebook.com
  - docker run $GITLAB_REG_UR/$GITLAB_REG_USER/$GITLAB_REG_IMAGE:latest microsoft.com